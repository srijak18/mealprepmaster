version: '3.8'
services:
  db:
    image: postgres:14
    environment:
      POSTGRES_USER: mealuser
      POSTGRES_PASSWORD: mealpass
      POSTGRES_DB: mealprepdb
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:

